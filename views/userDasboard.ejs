<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Hacktiv8 Forum</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f5f5f5;
      margin: 0;
      padding: 2rem;
    }

    .container {
      max-width: 800px;
      margin: auto;
    }

    .post {
      background: #fff;
      padding: 1.5rem;
      margin-bottom: 2rem;
      border-radius: 12px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.05);
      transition: all 0.2s ease-in-out;
    }

    .post:hover {
      box-shadow: 0 6px 16px rgba(0,0,0,0.1);
    }

    .post h2 {
      margin-top: 0;
      font-size: 1.5rem;
      color: #333;
    }

    .post p {
      color: #555;
      line-height: 1.6;
    }

  </style>
</head>
<body>
  <div class="container">
      <% posts.forEach(el => { %>
        <div class="post">
        <h2><a href="/post/<%= el.id %>"><%= el.title %></h2></a>
        <p><%= el.body %></p>
    </div>
    <% }) %>
  </div>
</body>
</html>
